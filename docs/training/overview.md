# Training subsystem

Goal: config-driven training runs with artifacts.

Planned capabilities:
- SFT baseline
- LoRA/QLoRA fine-tuning
- DPO
- distillation
- run registry + artifact outputs

Entry points will live under `src/frontier_ml_stack/training/`.