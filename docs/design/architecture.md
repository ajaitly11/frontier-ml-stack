# Architecture

Design principles:
- single repo, clear subsystem boundaries
- reproducible runs via configs + deterministic artifacts
- evaluation as a gate (regression-aware)
- measurable inference performance (latency/throughput)